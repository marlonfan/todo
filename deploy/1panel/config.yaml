server:
  port: 8080
  host: "0.0.0.0"

database:
  driver: sqlite
  dsn: "/app/data/todo.db"

jwt:
  secret: "change-this-secret-in-production"
  expire: 72h

notify:
  check_interval: 60s

plugins:
  telegram:
    enabled: true
  ntfy:
    enabled: true
  webhook:
    enabled: true
